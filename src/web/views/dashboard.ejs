<!DOCTYPE html>
<html>
  <head>
    <title>控制台 - <%= user.username %></title>
  </head>
  <body style="font-family: sans-serif; text-align: center; padding: 50px;">
    <h1>🎮 控制面板</h1>
    <p>歡迎回來，<%= user.username %>#<%= user.discriminator %></p>
    <h3>你加入的伺服器：</h3>
    <ul>
      <% user.guilds.forEach(guild => { %>
        <li><%= guild.name %></li>
      <% }) %>
    </ul>
    <a href="/logout">登出</a>
  </body>
</html>
